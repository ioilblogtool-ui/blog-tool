{
  "name": "blog-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "og:generate": "node scripts/generate-og.mjs",
    "check": "astro check",
    "check:all": "astro check && node scripts/check-category-mapping.mjs",
    "check:mapping": "node scripts/check-category-mapping.mjs",
    "fix:mapping": "node scripts/check-category-mapping.mjs --fix"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^5.5.5",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "sass": "^1.98.0"
  }
}
